# One-time owner-run Drizzle migration connection for hosted Supabase.
# Copy to .env.live.local, fill it from Supabase Dashboard > Connect, and never commit it.
# Use the direct connection or the Session pooler on port 5432 as the postgres role.
# Reserved password characters must be percent-encoded.
DATABASE_LIVE_MIGRATION_URL=
